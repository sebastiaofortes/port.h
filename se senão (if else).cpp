#include "port.h"
principal(){
   inteiro a;
    escrever("digite um número inteiro:");  
   ler("%d", &a);
   se(a >= 0){
   printf("número positivo ou nulo\n");     
        }   
   senao { 
   escrever("número negativo\n");      
            }
   sistema("pause");
            }
